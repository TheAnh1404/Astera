# Astera

Application source is split into:

- [`frontend`](./frontend): the Vite/React user interface;
- [`backend`](./backend): the FastAPI API, workers, migrations, and integration tests.

Start frontend development with:

```bash
cd frontend
npm run dev
```

Backend setup and runtime commands are documented in [`backend/README.md`](./backend/README.md).
